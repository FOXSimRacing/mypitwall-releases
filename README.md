# myPitWall Agent — Releases

Compiled binaries for the myPitWall Agent (Windows installer), published automatically by CI from
the private `FOXSimRacing/myPitWall` repository on every version tag.

This repository contains no source code — only release artifacts (`.exe` + `manifest.json`) used
by the Agent's own auto-update mechanism.

## Latest Release

<!-- release-notes:start -->
## 0.14.1

### Patch Changes

- 25b12e5: Internal refactor: extract the agent's HTTP/WS/telemetry/tunnel wiring into a reusable `startAgentCore()` and add a bare Electron shell scaffold (dev-only smoke test, not yet part of the packaged build). No user-facing behavior change — first step of the Electron migration that will eventually replace the fragile system-tray implementation.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR while this changeset (or any other `electron-migration-phase-*` changeset) is pending.** Starting with this phase, the still-packaged (non-Electron) pkg/Inno-Setup build's tray/auto-opened-window/autostart wiring is being migrated to `electron/main.ts`, which isn't yet what gets packaged for release (that cutover is Phase 6/7 of the Electron migration, tracked in issues #121-#128). Releasing before Phase 6/7 completes would ship a build silently missing those features. Wait for an explicit go-ahead once the full migration is validated end-to-end.
- 96811d5: Internal refactor: replace `node-systray-v2` with Electron's native Tray API, wired into the Electron shell from Phase 1. Root-cause fix for the recurring system-tray crash loop.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR while this changeset (or any other `electron-migration-phase-*` changeset) is pending.** As of this phase, the still-packaged (non-Electron) pkg/Inno-Setup build has no tray, no auto-opened window, and no autostart at all — that wiring now lives only in `electron/main.ts`, which isn't yet what gets packaged for release (that cutover is Phase 6/7 of the Electron migration, tracked in issues #121-#128). Releasing before Phase 6/7 completes would ship a build silently missing those features. Wait for an explicit go-ahead once the full migration is validated end-to-end.
- 51bf17b: Internal refactor: replace the old fake-native-window hacks (default-browser `--app=` launch, PowerShell/Win32 focus-and-minimize-watcher polling) and reg.exe-based autostart with Electron's native `BrowserWindow` and `app.setLoginItemSettings`. The close (X) button now hides to the tray instead of quitting.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR while this changeset (or any other `electron-migration-phase-*` changeset) is pending.** The still-packaged (non-Electron) pkg/Inno-Setup build has no tray, no auto-opened window, and no autostart at all until the Electron packaging cutover (Phase 6/7, tracked in issues #121-#128) lands. Wait for an explicit go-ahead once the full migration is validated end-to-end.
- 50d4ae6: Internal refactor: add Electron-native crash recovery (`app.relaunch()` with a fast-crash-loop cutoff, renderer-crash recovery via reload, single-instance lock) for the Electron shell, replacing the old process-respawning crash guard for that path only.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR while this changeset (or any other `electron-migration-phase-*` changeset) is pending.** The still-packaged (non-Electron) pkg/Inno-Setup build has no tray, no auto-opened window, and no autostart at all until the Electron packaging cutover (Phase 6/7, tracked in issues #121-#128) lands. Wait for an explicit go-ahead once the full migration is validated end-to-end.
- 727d703: Internal refactor: replace the custom manifest.json + Inno Setup silent-install auto-update flow with `electron-updater`, wired into the Electron shell. Feed format moves to `electron-updater`'s own `latest.yml` (generated by the electron-builder packaging cutover, Phase 6/7) — same publish target, `FOXSimRacing/mypitwall-releases`.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR while this changeset (or any other `electron-migration-phase-*` changeset) is pending.** The still-packaged (non-Electron) pkg/Inno-Setup build has no tray, no auto-opened window, no autostart, and now no update mechanism at all (its own auto-updater was deleted, since it fully moved to the Electron shell) — until the Electron packaging cutover (Phase 6/7, tracked in issues #121-#128) lands. Wait for an explicit go-ahead once the full migration is validated end-to-end.
- 69696de: Internal refactor: replace the esbuild + `@yao-pkg/pkg` + Inno Setup packaging pipeline with `electron-builder` (NSIS target) — the cutover the Electron migration has been building toward. `yarn package:win` now produces a working, verified-by-hand installer (tray, real telemetry, autostart, cloudflared tunnel, clean install/uninstall to `%LocalAppData%\Programs\myPitWall` with no admin) with tray/window/autostart/update mechanisms all present again.

  **⚠️ Release-safety note: do not merge the "Version Packages" PR yet.** This phase fixes local/manual packaging (`yarn package:win`), but `.github/workflows/release-agent.yml` still references the old pipeline's output paths and hand-rolled `manifest.json` — it will fail as-is. That CI update is Phase 7 (tracked in issue #128), the last piece of the migration (issues #121-#128). Wait for confirmation that Phase 7 is done and validated before merging "Version Packages" / cutting a release.
<!-- release-notes:end -->
